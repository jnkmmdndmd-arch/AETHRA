# Troubleshooting

If the world appears dark, check the graphics backend and ensure the project is launched with the Compatibility renderer on low-end hardware.

If remote players cannot connect, verify UDP port 31001 is reachable and both clients use the same protocol version.

If the authentication service is not configured, local/singleplayer mode still works; remote account login is unavailable until the service URL is configured.

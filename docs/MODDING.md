# Modding / Extensibility

Blocks, items and recipes are registry/data driven. New types should be added to the registry rather than branching game logic throughout the renderer.

Creature behavior is isolated in `scripts/entities`.
